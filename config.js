module.exports = {
    token: '',
    interval: 100,

    /**
     * The id of the target server
     * @string
     */
    guildId: '',

    /**
     * The highest role id (the script will not change the roles above it)
     * @string
     */
    highestRoleId: ''
}
